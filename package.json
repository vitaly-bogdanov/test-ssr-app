{
  "name": "ssr-app",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "server-dev": "yarn --cwd ./express-app dev",
    "client-dev": "yarn --cwd ./next-app dev",
    "dev": "concurrently \"yarn server-dev\" \"yarn client-dev\""
  },
  "dependencies": {
    "concurrently": "^6.0.2"
  }
}
